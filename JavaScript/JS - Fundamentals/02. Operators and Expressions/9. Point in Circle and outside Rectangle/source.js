var xCoordinate,
    yCoordinate,
    isOutOfRectangle = false,
    isInTheCircle = false,
    xPow,
    yPow,
    rPow = Math.pow(3, 2);

xCoordinate = 1;
yCoordinate = 4;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 3;
yCoordinate = 2;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 0;
yCoordinate = 1;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 4;
yCoordinate = 1;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 2;
yCoordinate = 0;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 4;
yCoordinate = 0;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 2.5;
yCoordinate = 1.5;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = 3.5;
yCoordinate = 1.5;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
isOutOfRectangle = false;
isInTheCircle = false;

xCoordinate = -100;
yCoordinate = -100;
xPow = Math.pow((xCoordinate - 1), 2);
yPow = Math.pow((xCoordinate - 1), 2);
if ((xCoordinate < -1 || xCoordinate > 5) || (yCoordinate < -1 || yCoordinate > 1)){
    isOutOfRectangle = true;
}
if ((xPow + yPow) < rPow) {
    isInTheCircle = true;
}
console.log('x = ' + xCoordinate + '; y = ' + yCoordinate + '; ' + (isInTheCircle && isOutOfRectangle ? 'inside K & outside of R - YES' : 'inside K & outside of R - NO'));
